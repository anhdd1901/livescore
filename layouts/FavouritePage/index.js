import React from 'react';
import {Text, ScrollView} from 'react-native';

export default function index() {
  return (
    <ScrollView style={{backgroundColor: '#0c0c0c'}}>
      <Text style={{color: 'white'}}>Favourite Page</Text>
    </ScrollView>
  );
}
